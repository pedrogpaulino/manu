# Fixtures de analisadores

Os arquivos deste diretório são pequenos, determinísticos e não contêm
segredos reais. O CAR usado nos testes é criado em memória com membros
ordenados, para que o fixture binário não dependa de timestamps ou do utilitário
`zip` do ambiente.
